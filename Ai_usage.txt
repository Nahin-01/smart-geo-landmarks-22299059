AI USAGE DISCLOSURE
Student ID: 22299059

Tool used: Claude (Anthropic), chat interface.

Scope of AI assistance:
- The entire Flutter/Dart codebase in app/lib/ (models, services,
  providers, screens, widgets) was generated by Claude directly from the
  exam PDF's functional requirements, based on a single prompt describing
  the assignment and asking for a complete implementation in Dart with
  no backend of my own.
- The API integration (endpoints, parameters, async job/poll flow) was
  written to match the "Available API Endpoints" section of the exam PDF
  exactly (get_landmarks, visit_landmark, get_job_status, create_landmark,
  delete_landmark, restore_landmark), using my student ID (22299059) as
  the API key.
- The offline/background-work architecture (SQLite repository pattern,
  WorkManager periodic + one-off polling, offline visit queue with
  connectivity-triggered sync) was designed and written by Claude to
  satisfy Requirements 8 and 10 of the exam sheet.
- The README.txt content (overview, features, API usage, offline
  strategy, architecture, challenges) was drafted by Claude based on the
  generated code.
- The Android project shell (gradle files, native android/ios scaffolding
  beyond AndroidManifest.xml) was NOT generated by AI in this submission
  and must be produced locally via `flutter create`, since the sandbox
  used to prepare this submission had no Flutter SDK / no access to
  pub.dev to run and verify a full build.

What I did myself / will do myself:
- Reviewed the generated code and this disclosure.
- Will run `flutter create`, `flutter pub get`, and `flutter run` locally
  to build, test on a device/emulator, and fix any issues that surface
  only at build/runtime (package version conflicts, minor API response
  shape mismatches from my personal API key's data, etc.), since the
  code was written without the ability to compile it against the live
  Flutter toolchain or my personal API key's actual response shapes.
- Will verify against my own API key's live responses and adjust field
  parsing in lib/models/landmark.dart / lib/services/api_service.dart if
  my key's JSON differs from the documented shape.
